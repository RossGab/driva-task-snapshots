name: Snapshot Mid

on:
  workflow_dispatch:
    inputs:
      force_run:
        description: "Force run outside hours (1 = yes)"
        required: false
        default: "0"

  schedule:
  # 2× per day (PH 6AM and 6PM)
    - cron: "0 22,10 * * *"

# 🔒 REQUIRED: shared lock for ALL snapshot workflows
concurrency:
  group: snapshot-main
  cancel-in-progress: false

jobs:
  snapshot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: npm install firebase-admin

      - name: Run snapshot
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FORCE_RUN: ${{ github.event.inputs.force_run }}
        run: node scripts/snapshot-mid.js

      # ✅ SAFE COMMIT (NO RACE CONDITIONS)
      - name: Commit snapshots
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add snapshots

          # No changes → exit cleanly
          if git diff --cached --quiet; then
            echo "No snapshot changes"
            exit 0
          fi

          git commit -m "snapshot: mid"

          # 🔥 REQUIRED to avoid fetch-first error
          git pull --rebase origin main

          git push
